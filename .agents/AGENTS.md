# Workspace Rules

- **Planning Locality**: When instructed to create a plan based on a context, reference file, or template, ALWAYS save the `implementation_plan.md` AND `walkthrough.md` directly into the same directory where that source/reference file exists (e.g., `Chapter_02_Prompt_Engineering` instead of only using the brain artifacts folder).
